name: Formatter
on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  julia-format:
    name: Formatter
    runs-on: ubuntu-latest
    steps:
      - uses: julia-actions/setup-julia@latest
      - uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install JuliaFormatter and format
        run: |
          # Pin: JuliaFormatter floats by default and normalization rules have shifted
          # across releases (2.x drift). Tree verified format-clean under 2.13.0;
          # pinning makes the check deterministic — bump deliberately with a
          # repo-wide format pass when needed.
          julia --color=yes -e 'using Pkg; Pkg.add(PackageSpec(name="JuliaFormatter", version="2.13"))'
          julia --color=yes -e 'using JuliaFormatter; format(".", verbose=true)'
      - name: Commit formatted code
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet && exit 0
          git add -A
          git commit -m "chore: autoformat"
          git push
      - name: Format check
        if: github.event_name != 'workflow_dispatch'
        run: |
          julia --color=yes -e '
          out = Cmd(`git diff --name-only`) |> read |> String
          if out == ""
            exit(0)
          else
            @error "Some files have not been formatted !!!"
            write(stdout, out)
            exit(1)
          end'
